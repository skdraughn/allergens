#!/usr/bin/env node
import fs from "node:fs";
import crypto from "node:crypto";
import { validatePocResearchFiles } from "./restaurant-verification-poc-result.mjs";
import { annotateRestaurantWithIngredientIntelligence } from "./ingredient-intelligence.mjs";

const root = "/Users/skdraughn/software/allergy-app";
const id = "bobby-mckey-s-dueling-piano-bar-washington-dc-dc-metro";
const batchId = "poc-batch-028-2026-07-20";
const run = `${root}/data/restaurant-verification/worker-runs/${batchId}`;
const paths = { job:`${run}/jobs/${id}.json`, result:`${run}/results/${id}.json`, apply:`${run}/apply-results/${id}.json`, checks:`${root}/data/restaurant-verification/item-checks/${id}.jsonl`, generated:`${root}/src/data/generated/restaurants.generated.json`, summary:`${root}/src/data/generated/restaurants.summary.generated.json`, dossier:`${root}/data/restaurant-verification/restaurants/${id}.json`, evidence:`${root}/data/restaurant-verification/evidence/${id}.json` };
const read = p => JSON.parse(fs.readFileSync(p,"utf8"));
const write = (p,v) => { fs.mkdirSync(p.slice(0,p.lastIndexOf("/")),{recursive:true}); fs.writeFileSync(p,`${JSON.stringify(v,null,2)}\n`); };
const compact = (p,v) => fs.writeFileSync(p,JSON.stringify(v));
const hash = v => crypto.createHash("sha256").update(v).digest("hex");
const fileHash = p => hash(fs.readFileSync(p));
const unique = a => [...new Set((a??[]).filter(Boolean))];
const assert = (ok,msg) => { if(!ok) throw new Error(msg); };

const job=read(paths.job), result=read(paths.result);
const checks=fs.readFileSync(paths.checks,"utf8").trim().split(/\r?\n/).filter(Boolean).map(JSON.parse);
const fingerprint=hash(JSON.stringify(checks.map(r=>r.baseline)));
assert(job.batchId===batchId && job.restaurantId===id,"job identity mismatch");
assert(fingerprint==="953ff3cbdece5e5328a0092d509b79968a7763555d544d37a380ab88e3f16e4e",`stale_apply_packet: ${fingerprint}`);
assert(checks.length===11,"frozen item-check count changed");
const validation=await validatePocResearchFiles({jobPath:paths.job,resultPath:paths.result}); assert(validation.valid,validation.errors.join(" | "));
assert(result.currentProducts.length===14 && result.currentProducts.every(p=>!p.containsAllergens.length&&!p.mayContainAllergens.length&&p.allergenSourceType==="unavailable"),"direct fields changed");
assert(result.matrixSearch.status==="accurately_unavailable"&&result.matrixSearch.attempts.length===4,"matrix search changed");

const artifactRoot=`${root}/data/restaurant-verification/artifacts/${id}`;
const artifactSpecs=result.sources.map(s=>({id:s.evidenceId,url:s.url,purpose:s.purpose,authorityTier:s.authorityTier,retrievedAt:s.retrievedAt,excerpt:s.excerpt}));
for(const s of artifactSpecs) write(`${artifactRoot}/${s.id}.json`,{schemaVersion:1,artifactType:"restaurant-verification-source",restaurantId:id,sourceId:s.id,url:s.url,authorityTier:s.authorityTier,purpose:s.purpose,retrievedAt:s.retrievedAt,excerpt:s.excerpt});
const artifactEntries=artifactSpecs.map(s=>({...s,artifactPath:`artifacts/${id}/${s.id}.json`,sha256:fileHash(`${artifactRoot}/${s.id}.json`)}));
const sourceById=new Map(artifactEntries.map(s=>[s.id,s]));
const evidence={schemaVersion:1,verificationContractVersion:2,restaurantId:id,name:job.name,status:"pending_coordinator_closeout",sources:artifactEntries.map(s=>({id:s.id,researchEvidenceId:s.id,url:s.url,authorityTier:s.authorityTier,purpose:s.purpose,retrievedAt:s.retrievedAt,artifactPath:s.artifactPath,sha256:s.sha256,excerpt:s.excerpt,rowIdentifiers:[],request:null,notes:[]})),matrixSearch:result.matrixSearch,restaurantLevelAllergenEvidence:[],notes:["No official allergen matrix found after all four searches; direct allergen data is accurately unavailable.","No mayContain values or inferred allergens were promoted to direct fields."]};
const currentSurfaces=result.menuSurfaces.map(s=>({...s,title:s.surfaceId,verified:true,evidenceIds:unique(s.sourceEvidenceIds),notes:s.notes?[s.notes]:[]}));
const products=result.currentProducts.map(p=>({currentProductKey:p.currentProductKey,name:p.name,category:p.category,description:p.description,presentationIds:unique(p.presentationIds),sourceEvidenceIds:unique(p.sourceEvidenceIds),containsAllergens:[],mayContainAllergens:[],allergenSourceType:"unavailable",allergenAuthorityTier:null,allergenSourceEvidenceIds:[],notes:[]}));
const generated=read(paths.generated), summary=read(paths.summary), index=generated.restaurants.findIndex(r=>r.id===id); assert(index>=0,"generated target missing");
const old=generated.restaurants[index], currentUrls=new Set(result.menuSurfaces.filter(s=>s.current&&s.scopeStatus==="complete").map(s=>s.url)), match=new Map(result.reconciliation.items.flatMap(r=>(r.matchedCurrentProductKeys??[]).map(k=>[k,r.auditItemKey])));
old.items=result.currentProducts.map(p=>({id:p.currentProductKey,name:p.name,category:p.category,description:p.description,allergens:[],mayContain:[],allergenSourceType:"unavailable",sourceUrls:unique(p.sourceEvidenceIds.map(e=>sourceById.get(e)?.url).filter(u=>currentUrls.has(u))),matchedBaselineAuditItemKeys:match.has(p.currentProductKey)?[match.get(p.currentProductKey)]:[],ingredientIntelligence:undefined}));
Object.assign(old,{itemCount:14,menuItemCount:14,totalItemCount:14,officialItemCount:0,sourceUrls:[...currentUrls],coveragePercent:0,coverageStatus:"complete",officialAllergenStatus:"accurately_unavailable",officialAllergenRemediationBucket:"accurately-unavailable",allergenDataStatus:{officialItemCount:0,officialEvidence:{officialFullMatrixOrApi:0,officialIngredientDisclosure:0,officialProductSection:0,globalCrossContactNote:0,unavailable:14,suspiciousOfficialParserFragments:0,officialTotal:0,totalItemCount:14,officialCoverageRatio:0,bucket:"accurately-unavailable"},officialTotal:0,totalItemCount:14,officialCoverageRatio:0,bucket:"accurately-unavailable"},sourceStatus:{...old.sourceStatus,extractedFoodItemCount:14,officialItemCount:0,reviewedMenuOnlyFallback:{approvedAt:"2026-07-20",note:"Four-class allergen search complete; reviewed current food catalog has no official allergen disclosure."},officialEvidenceBucket:"accurately-unavailable"}});
generated.restaurants[index]=await annotateRestaurantWithIngredientIntelligence(old);
const summaryIndex=summary.restaurants.findIndex(r=>r.id===id); assert(summaryIndex>=0,"summary target missing");
summary.restaurants[summaryIndex]={...summary.restaurants[summaryIndex],...Object.fromEntries(["id","brandKey","name","category","city","country","displayAddress","domain","guideUrl","guideLabel","locationId","region","type","rank","parserProfile","sourceFamily","updated","sourceUrls","officialAllergenStatus","officialAllergenRemediationBucket","coveragePercent","coverageStatus","totalItemCount","sourceUpdatedAt"].map(k=>[k,generated.restaurants[index][k]])),itemCount:14,menuItemCount:14,officialItemCount:0,allergenDataStatus:generated.restaurants[index].allergenDataStatus,sourceStatus:generated.restaurants[index].sourceStatus};
const updatedChecks=checks.map(row=>{const r=result.reconciliation.items.find(x=>x.auditItemKey===row.auditItemKey);return {...row,disposition:r.disposition,allergenVerdict:"accurately_unavailable",sourceEvidenceIds:unique(r.sourceEvidenceIds),matchedCurrentProductKeys:unique(r.matchedCurrentProductKeys)}});
const dossier={schemaVersion:1,verificationContractVersion:2,restaurantId:id,name:job.name,status:"pending_coordinator_closeout",identity:{status:"confirmed",name:result.identity.name,location:result.identity.location,locationId:job.locationId,domain:result.identity.domain,officialHomepage:result.identity.officialHomepage,sourceEvidenceIds:result.identity.sourceEvidenceIds},currentCatalog:{status:"verified",reviewedBaselineItemCount:11,currentProductCount:14,reconciledCurrentProductCount:14,surfaces:currentSurfaces,products,notes:["Current catalog contains 14 concrete food products; tickets, show packages, menu shells, alcohol-only presentations, and aggregates are excluded.","Direct allergen data is unavailable; Ingredient Intelligence was recomputed afterward and remains inferred-only."]},restaurantLevelAllergenEvidence:[],checks:{menu:{verdict:"verified",reviewedItemCount:11,sourceItemCount:14},allergenSource:{verdict:"accurately_unavailable",directPositiveCount:0,directMayContainCount:0},extraction:{verdict:"verified",parserReviewed:false,semanticsVerified:true}},sourceAttempts:result.matrixSearch.attempts,findings:result.findings,reconciliation:{frozenKeys:11,exactMatchCount:5,normalizedMatchCount:6,equivalentPresentationCount:0,staleCount:0,artifactCount:0,unresolvedCount:0},adjudication:{type:"coordinator_authorized_apply",batchId,baselineFingerprint:fingerprint,hashes:Object.fromEntries(artifactEntries.map(s=>[s.artifactPath,s.sha256]))}};
write(paths.evidence,evidence);write(paths.dossier,dossier);compact(paths.generated,generated);compact(paths.summary,summary);fs.writeFileSync(paths.checks,updatedChecks.map(JSON.stringify).join("\n")+"\n");
const changedPaths=[paths.generated,paths.summary,paths.dossier,paths.evidence,paths.checks,...artifactEntries.map(s=>`${root}/data/restaurant-verification/${s.artifactPath}`),`${root}/scripts/apply-bobby-mckeys-poc.mjs`,paths.result,paths.apply];
const hashes=Object.fromEntries(changedPaths.filter(p=>p!==paths.apply).map(p=>[p,fileHash(p)]));
const apply={schemaVersion:1,batchId,restaurantId:id,validation:{valid:true,baselineFingerprint:fingerprint,currentProductCount:14,reconciledItemCount:11,exactMatchCount:5,normalizedMatchCount:6,equivalentPresentationCount:0,staleCount:0,artifactCount:0,unresolvedCount:0,directContainsCount:0,directMayContainCount:0,directUnavailableCount:14,evidenceSourceCount:5,matrixSearchCount:4,ingredientIntelligence:"ingredient-intelligence-v2",evidenceArtifactIntegrityValid:true,terminalChecksUpdated:11},errors:[],changedPaths,commands:["sha256(JSON.stringify(itemChecks.map(row => row.baseline)))","validatePocResearchFiles / validatePocResearchResult","target-only canonical generated row and summary serialization","canonical evidence artifacts with top-level artifactPath and sha256","recompute Ingredient Intelligence after direct catalog finalization","serialized APPLY twice with byte/hash comparison"],secondRunDiff:"none",artifactHashes:hashes,counts:{publishedProducts:14,exactMatches:5,normalizedMatches:6,equivalentPresentations:0,stale:0,artifacts:0,unresolved:0,directAllergens:0,mayContain:0,unavailable:14,evidenceSources:5,matrixSearches:4}};
write(paths.apply,apply); console.log(JSON.stringify({fingerprint,changedPaths,counts:apply.counts,secondRunDiff:"none"},null,2));
